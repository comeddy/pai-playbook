name: Canary Test
description: Minimal known-good form from GitHub docs (temporary diagnostic)
body:
  - type: input
    id: contact
    attributes:
      label: Contact Details
      description: How can we get in touch with you if we need more info?
      placeholder: ex. email@example.com
    validations:
      required: false
